using UnityEngine;
using System.Collections;

public class animationDisplayLocation : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Destroy (this.gameObject, 12f);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
