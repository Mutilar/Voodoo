using UnityEngine;
using System.Collections;

public class ExploderSlow : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Destroy (this.gameObject, 1.56f);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
